# Legacy, Not Deployed

This directory is an undeployed snapshot from the portfolio's earlier build system. Astro ignores it.

Do not use files in this directory as current evidence about Kevin Astuhuaman. The maintained source of truth is `src/data/site.js`; `npm run build` generates the public machine corpus in `site-public/` and the deployable site in `dist/`.
